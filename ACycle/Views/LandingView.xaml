<?xml version="1.0" encoding="utf-8" ?>
<views:ContentPageBase
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ACycle.Views"
    xmlns:viewModels="clr-namespace:ACycle.ViewModels"
    x:Class="ACycle.Views.LandingView"
    x:DataType="viewModels:LandingViewModel">

    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <Label Text="{x:Binding Heading}" />
        <Label Text="{x:Binding Description}" />
    </VerticalStackLayout>
</views:ContentPageBase>
